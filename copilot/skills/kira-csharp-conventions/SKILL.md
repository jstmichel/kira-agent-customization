---
name: kira-csharp-conventions
description: "C# constructor and static method conventions. Use when writing any C# class in any layer. Rules: primary constructors by default; static methods when no instance data is used; static class when all methods are static and no state is held."
---

# C# Conventions

Use primary constructors by default. Favor static methods when no instance data is used. Use a static class when all methods are static and no state is held.
